<nav class="backlinks">
    <ul>
        {% for inbound in backlinks %}
            <li>
                <a href="{{ path('app_wiki', {title:inbound.title}) }}">
                    <i class="icon-link"></i>
                    {{ inbound.title }}
                </a>
            </li>
        {% endfor %}
    </ul>
</nav>