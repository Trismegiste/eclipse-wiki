{# show orphans #}
{% extends "base.html.twig" %}

{% block title %}
    Vertex orphelins
{% endblock %}

{% block header_title %}{{ block('title') }}{% endblock %}

{% block content %}
    <ul>
        {% for vertex in orphan %}
            <li>
                <a href="{{ path('app_vertexcrud_show', {pk:vertex.pk}) }}">{{ vertex.title }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}

