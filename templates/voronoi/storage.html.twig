{% extends "voronoi/baseform.html.twig" %}

{% block content %}
    <div class="pure-g">
        <div class="pure-u-1 pure-u-xl-2-3">
            <img src="{{ path('app_voronoicrud_generate', {pk: form.vars.data.pk, fog: 0}) }}" class="pure-img"/>
        </div>
        <div class="pure-u-1 pure-u-xl-1-3">
            {{ form(form) }}
            <a href="{{ path('app_voronoicrud_runmap', {pk: form.vars.data.pk}) }}" class="pure-button" style="width: 100%;" target="_blank">Tester la battlemap</a>
        </div>
    </div>
{% endblock %}
