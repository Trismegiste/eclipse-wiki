<div class="pure-g">
    <div class="pure-u-1-12 thumb">
        <i class="icon-hexamap"></i>
    </div>
    <div class="pure-u-7-12">
        <a href="{{ path('app_vertexcrud_show', {pk: item.pk}) }}" class="block-link">
            <h2>{{ item.title }}</h2>
            <span>Battlemap {{ item.side }}×{{ item.side }} - {{ item.container.name|trans }}</span>
        </a>
    </div>
    <div class="pure-u-1-3">
        <nav class="listing-action">
            {{ include('voronoi/actions.html.twig', {vertex:item}) }}
        </nav>
    </div>
</div>