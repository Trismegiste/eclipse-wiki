{% extends "base.html.twig" %}

{% block title %}Renommer {{ form.vars.data.focus.title }}{% endblock %}

{% block header_title %}
    {# the header menu with title #}
    <nav style="float: right">
        {{ include('vertex/actions.html.twig', {vertex: form.vars.data.focus}) }}
    </nav>   
    {% block header_big_title %}{{ block('title') }}{% endblock %}
{% endblock %}

{% block content %}
    <h2>Voulez-vous renommer <strong>{{ form.vars.data.focus.title }}</strong> ?</h2>
    <span>Ce vertex est mentionné dans les documents suivants :</span>
    {{ include('fragment/wiki_backlinks.html.twig', {backlinks: form.vars.data.inbound}) }}
    {{ form(form) }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .backlinks ul li {
            display: block;
        }
    </style>
{% endblock %}