<div class="pure-g">
    <div class="pure-u-1-4 pure-u-xxl-1-12 thumb">
        <i class="icon-loveletter"></i>
    </div>
    <div class="pure-u-3-4 pure-u-xxl-2-3">
        <a href="{{ path('app_vertexcrud_show', {pk: item.pk}) }}" class="block-link">
            <h2>{{ item.title }}</h2>
            <span>
                {{ item.category|capitalize }} pour {{ item.player }}
                {% if item.pcChoice is not empty %}(résolue){% endif %}
            </span>
        </a>
    </div>
    <div class="pure-u-1 pure-u-xxl-1-4">
        <nav class="listing-action">
            {{ include('loveletter/actions.html.twig', {vertex:item}) }}
        </nav>
    </div>
</div>
