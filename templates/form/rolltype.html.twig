{# Custom rendering for RollType #}
{% block roll_widget %}
    <div class="pure-g">
        <div class="pure-u-1-2">{{ form_widget(form.trait, {attr: {class: 'pure-input-1'}}) }}</div>
        <div class="pure-u-1-2">{{ form_widget(form.difficulty, {attr: {class: 'pure-input-1'}}) }}</div>
    </div>
{% endblock %}