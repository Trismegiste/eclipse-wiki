{# dump history #}
{% if document.hasPinnedTimeline %}
    {% set timeline = document.timeline %}
    <section class="timeline-toc">
        <a href="{{ path('app_vertexcrud_show', {pk: timeline.pk}) }}"
           <i class="icon-movie-roll"></i>
            {{ timeline.title }}
        </a>
    </section>
{% endif %}