<div class="pure-g">
    <div class="pure-u-1-12 thumb">
        <i class="{{ char_icon(item) }}"></i>
    </div>
    <div class="pure-u-17-24">
        <a href="{{ path('app_npcgenerator_show', {pk: item.pk}) }}" class="block-link">
            <h2>{{ item.title }}</h2>
            <span>{{ item.description }}. Morphe: {{ item.morph.title }} ({{ item.powerIndex }})</span>
        </a>
    </div>
    <div class="pure-u-5-24">
        <nav class="listing-action">
            {{ include('npc/actions.html.twig', {npc:item}) }}
        </nav>
    </div>
</div>