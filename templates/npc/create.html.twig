{% extends "base.html.twig" %}

{% block header_title %}Création de PNJ{% endblock %}

{% block content %}
    {{ form_start(form) }}
    <div class="pure-g character">
        <div class="pure-u-1">
            {{ form_widget(form['name'], {attr:{class:'pure-input-1'}}) }}
        </div>
        <div class="pure-u-1-3">
            <h2>Historique</h2>
            {{ form_widget(form['background'],  {attr: {
                                                    'data-url': path('app_npcgenerator_getbackground'), 
                                                    'data-detail': 'background_detail'
                                                }}) }}
            <section id="background_detail"></section>
        </div>
        <div class="pure-u-1-3">
            <h2>Faction</h2>
            {{ form_widget(form['faction'],  {attr: {
                                                    'data-url': path('app_npcgenerator_getfaction'), 
                                                    'data-detail': 'faction_detail'
                                                }}) }}
            <section id="faction_detail"></section>
        </div>
        <div class="pure-u-1-3">
            <h2>Morphe</h2>
            {{ form_widget(form['morph'],  {attr: {
                                                    'data-url': path('app_npcgenerator_getmorph'), 
                                                    'data-detail': 'morph_detail'
                                                }}) }}
            <section id="morph_detail"></section>
        </div>            
    </div>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function () {

            function selectUpdate(event) {
                let param = event.target.dataset
                document.getElementById(param.detail).innerHTML = '<i class="icon-spin3 animate-spin"></i>'
                fetch(param.url + '?key=' + event.target.value)
                        .then(function (resp) {
                            return resp.text()
                        })
                        .then(function (data) {
                            document.getElementById(param.detail).innerHTML = data
                        })
            }

            document.getElementById('npc_background').addEventListener('change', selectUpdate)
            document.getElementById('npc_faction').addEventListener('change', selectUpdate)
            document.getElementById('npc_morph').addEventListener('change', selectUpdate)
        })
    </script>
{% endblock %}