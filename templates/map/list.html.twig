{% extends "base.html.twig" %}

{% block content %}
    <div class="pure-g">
        {% for map in template %}
            <div class="pure-u-1-6">
                {{ map.title }}
                {{ map|raw }}
            </div>
        {% endfor %}
    </div>
{% endblock %}