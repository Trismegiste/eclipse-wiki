<div class="pure-g">
    <div class="pure-u-1-4 pure-u-xl-1-12 thumb">
        <i class="icon-movie-roll {% if item.archived %}archived{% endif %}"></i>
    </div>
    <div class="pure-u-3-4 pure-u-xl-7-12">
        <a href="{{ path('app_vertexcrud_show', {pk: item.pk}) }}" class="block-link">
            <h2>{{ item.title }}</h2>
            <span>Scénario {% if item.archived %}archivé{% endif %}</span>
        </a>
    </div>
    <div class="pure-u-1 pure-u-xl-1-3">
        <nav class="listing-action">
            {{ include('timeline/actions.html.twig', {vertex:item}) }}
        </nav>
    </div>
</div>
